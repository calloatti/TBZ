*!* _apiDrawFrameControl

Lparameters hdc, lprc, uType, uState

Declare Integer DrawFrameControl in WIN32API As _apiDrawFrameControl ;
	Integer hdc, ;
	String  lprc, ;
	Integer uType, ;
	Integer uState

Return _apiDrawFrameControl(m.hdc, m.lprc, m.uType, m.uState)